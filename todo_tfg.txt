TODO's EN PROCESO:
    -conseguir leer de la DB las observaciones del usuario:
        -conseguir usuario en sesion    OK
        -conseguir consulta SQL para coger todas las observaciones, se quieren:
            todos los datos de las tablas observaciones, objetos y telescopios

            los datos de la observaciones:
                //consulta de todas las observaciones de un usuario(nombre)
                SELECT obs.* 
                FROM `observaciones` obs 
                WHERE created_by = 
                    (SELECT u.user_id 
                    FROM `usuarios` u 
                    WHERE u.username = 'manel');

            FULL CONSULTA: 
                //las observaciones, con los datos de su objeto respectivo, y los datos su telescopio respectivo, de un usuario concreto
                SELECT obs.*, obj.*, tl.*
                FROM `objetos` obj
                LEFT JOIN `observaciones` obs ON obj.objeto_id = obs.observed_object
                LEFT JOIN `telescopios` tl ON obs.telescopeUsed = tl.telescope_id
                WHERE obs.created_by = (SELECT u.user_id from `usuarios` u where u.username = 'manel');

        -crear php para peticion
            -hacer peticion
            -devolver JSON
        -procesar JSON en .js
        -rellenar datos de JSON en una tarjeta

----------------------------
MODIFICACIONES PENDIENTES: 

PRIORIZA FUNCIONALIDAD
-Responsiveness

-justificar Texto index

-en login/signin apliar campo email

-añadir calendario para reservar visitas(formulario)
    https://www.geeksforgeeks.org/how-to-add-a-datepicker-in-form-using-html/

-añadir favicon

-mejorar footer

-cambiar TU del navbar